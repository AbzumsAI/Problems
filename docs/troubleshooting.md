# Troubleshooting

When a script fails, I start with the first Python error.

Common checks:

- The required package is installed.
- The script is run from the repo root.
- The variable names match the expression.
- The file was saved before running.
- Plot windows are not hidden behind the editor.

After a fix, I run the changed file again.
