# Running scripts

Run scripts from the repo root so imports and relative paths behave the same way each time.

```powershell
python s1p1.py
```

Some scripts print values. Some scripts may open a plot window. Read the matching problem statement when the output looks short.
