# SymPy notes

Many scripts use SymPy for symbolic math.

I keep symbol names close to the written problem. That makes it easier to compare code with the board solution.

After changing a SymPy script, I compile it and run the file that changed.
