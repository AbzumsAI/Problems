# s4p8.py

I use this note to remember where `s4p8.py` fits in the class examples.

This file is a session 4, problem 8. It has 89 bytes in the current repo.

## Run

```powershell
python s4p8.py
```

## Check

After changing this file, I compile it with:

```powershell
python -m py_compile s4p8.py
```

If the script draws a plot, I also check the figure window or saved output by hand.
