# func.py

I use this note to remember where `func.py` fits in the class examples.

This file is a function example. It has 407 bytes in the current repo.

## Run

```powershell
python func.py
```

## Check

After changing this file, I compile it with:

```powershell
python -m py_compile func.py
```

If the script draws a plot, I also check the figure window or saved output by hand.
