# s2p-scoring.py

I use this note to remember where `s2p-scoring.py` fits in the class examples.

This file is a session 2 scoring note. It has 27 bytes in the current repo.

## Run

```powershell
python s2p-scoring.py
```

## Check

After changing this file, I compile it with:

```powershell
python -m py_compile s2p-scoring.py
```

If the script draws a plot, I also check the figure window or saved output by hand.
