# s1p5.py

I use this note to remember where `s1p5.py` fits in the class examples.

This file is a session 1, problem 5. It has 501 bytes in the current repo.

## Run

```powershell
python s1p5.py
```

## Check

After changing this file, I compile it with:

```powershell
python -m py_compile s1p5.py
```

If the script draws a plot, I also check the figure window or saved output by hand.
