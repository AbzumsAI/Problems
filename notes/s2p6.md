# s2p6.py

I use this note to remember where `s2p6.py` fits in the class examples.

This file is a session 2, problem 6. It has 1437 bytes in the current repo.

## Run

```powershell
python s2p6.py
```

## Check

After changing this file, I compile it with:

```powershell
python -m py_compile s2p6.py
```

If the script draws a plot, I also check the figure window or saved output by hand.
