# OpenFDA Drug Lookup

This example is a cleaned version of the drug lookup files from the chat export.

The network call is kept in `fetch_drug_info`, while tests cover URL building and JSON parsing without contacting the API.

Run it manually with:

```bash
python openfda_drug_lookup.py
```
